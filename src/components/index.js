import Sidebar from "./Sidebar/Sidebar";
import MovieCard from "./MovieCard/MovieCard";
import MoviePoster from "./MoviePoster/MoviePoster";

const useComponents = () => {
  return { Sidebar, MovieCard, MoviePoster };
};

export default useComponents;
